<view class="page">
    <view class="page-body">
        <view wx:if="{{sacanResult.isShow}}" class="weui-form-preview">
        </view>
        <view>
            <button type="primary" bind:tap="onscan">扫一扫</button>
        </view>
    </view>
</view>